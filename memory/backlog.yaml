# memory/backlog.yaml — the task queue the loop pulls from.
# Managed by `pb` (next --claim / record). Edit by hand to add tasks.
# status: todo | in_progress | blocked | done   priority: 1 = highest
# acceptance_checks: shell commands that must exit 0 before `record --status done` succeeds.
tasks:
  - id: plan-20260629-001
    title: Run morning attention research for project attention-research
    status: todo
    skill: daily-research-run
    mode: attention-research
    priority: 1
    acceptance_checks:
      - >-
        node scripts/attention-research-daily.mjs --project attention-research --window morning --date
        today --dry-run
